"""preempt — the pre-emptive analysis layer (the "store manager in the corner").

The PUSH surface: volunteers significance nobody asked for. See docs/THEORY.md (pre-emptive layer).
A **pure structural system** — no AI, no LLM. Every "insight" is a read-out of data geometry:
signed deviation from a zero-mean (significance), Monty-Hall elimination over the spatially-weighted
store/warehouse graph (prescription), Kuramoto coherence (co-movement), and Jeeves-mode structured
text when an axis is unresolved (speaking up). Read-only and advisory.

Deferred to Phase 2 (DESIGN.md §13): requires the local substrate + snapshot history first; the
banks / zero-point calibration / spatial-graph weights are designed from real data, not guessed.
"""
