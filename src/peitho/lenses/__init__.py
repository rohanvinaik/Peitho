"""lenses — the redundant node-network encodings across analytical frameworks (THE intelligence).

The local substrate is not a SQL warehouse; it is the same facts projected independently through
multiple banks/lenses (Sparse-Wiki style), whose redundancy is what enables hierarchical traversal
and deconvolution ("Jordan vs Jordan"). Signed-ternary + scalar-magnitude geometry, zero-point at
each bank's semantic mean, Bellman-Ford traversal (THEORY.md §1-4). The SKU wormhole (THEORY.md) is
the index / fast-path layer over these encodings. Redundant and self-checking: a broken or lossy
lens is outvoted by the others, not silently trusted (holographic over-determination).

Governing invariant: the geometry NOTICES what is worth saying; it never sources a quoted number
(that comes from the authoritative backend pull). Built over the immutable landing in `peitho.store`.

Deferred to real data: the actual lenses/banks, per-bank zero-point calibration, and the
store/warehouse spatial graph (node distance <-> map distance). See THEORY.md and DESIGN.md §13.

Naming: `lenses` (a.k.a. "banks" in the Sparse-Wiki idiom) — provisional; rename to `banks` if
preferred. Distinct from the retail "bank SET" (the ModelAtlas axes/query-plan vocabulary), which
is a different thing (DESIGN.md §7, §14).
"""
